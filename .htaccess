RewriteEngine On

# Keep existing files/directories accessible directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback for clean React Router URLs (e.g. /admin)
RewriteRule ^ index.html [L]
